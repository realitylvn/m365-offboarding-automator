# azd local environment state
.azure

# build-methodology notes (spec, implementation plan) — kept local, not published
docs/superpowers/

# never commit a private key
*.pfx
*.pem

# Pester output
**/testResults.xml
**/TestResults.xml

# compiled Bicep (use `az bicep build --stdout`)
infra/main.json
